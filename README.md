ZIO Service Service
-------------------

Kinda like runtime DI, but at compile time.

Run with STDOUT:
```
./sbt run
```

Run without STDOUT:
```
./sbt "run -q"
```

Test with `TestConsole`:
```
./sbt test
```
